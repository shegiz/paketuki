# Environment Configuration Example
# Copy this file to .env and adjust values
# Note: This project uses config.php instead, but .env can be used for sensitive data

DB_HOST=localhost
DB_PORT=3306
DB_NAME=parcel_lockers
DB_USER=root
DB_PASS=

APP_TIMEZONE=Europe/Vienna
APP_DEBUG=false
